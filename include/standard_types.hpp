#pragma once

namespace pasc {
    enum standard_type {
        est_unknown,
        est_integer,
        est_char,
        est_real,
        est_boolean,
        est_enum,
        est_procedure,
        est_function,
        est_array,
        est_record
    };
}